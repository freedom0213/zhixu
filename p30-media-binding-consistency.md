# P30 · 「讲师端说已上传、学生端说没视频」——两端判定口径统一

> 2026-09-18 · 用户报：1.3 的视频已经显示了，但**又给 2.1 加了视频后学生端还是不显示**，
> 感觉「两端不同步」。查下来不是同步问题，而是**同一个事实被三个地方用三套标准判断**。

## 1 · 真相：数据本身是「半截」的

2.1 小节在库里的状态：

| 表 | media_id | video_name | media_duration |
|---|---|---|---|
| 草稿表 | **NULL** | Wuthering Waves…mp4 | **0** |
| 正式表（学生端读的） | **NULL** | Wuthering Waves…mp4 | **0** |

而 `media` 表里躺着对应的上传记录（**id=9 / 8MB / 148 秒 / 同名文件**）——**上传是成功的，只是没挂到小节上**。

**为什么学生端看不到**：播放凭证接口按 `media_id` 查媒资，`media_id` 为 NULL → 「该小节暂未上传视频」。
**为什么讲师端说"已上传"**：`toSection()` 里的判定是 `video_name` 非空 → 显示「已配视频」。

```java
// 修复前：看的是文件名
String videoName = row.getVideoName();
if (videoName != null && !videoName.isEmpty()) { s.getVideo().setStatus("done"); ... }
```

**同一个事实、两个字段、两套标准** —— 讲师端看到的和学生端能不能播，根本不是同一个判断。

## 2 · 三个来源凑成这个结果

| # | 环节 | 问题 |
|---|---|---|
| 1 | **登记** | `videoName` 有值但 `mediaId` 缺失时，旧逻辑只是静默地写 NULL（P28 加的兜底没覆盖所有情况，也没有校验 payload 里的 id 是否真实存在） |
| 2 | **判定** | 讲师端看 `video_name`、学生端看 `media_id`，口径不一致 |
| 3 | **时长** | 前端预读视频元数据失败时传 0 → 库里就是 0（2.1 正是这个特征），目录显示 `0:00` |

> 补充：用户那次登记的 `media_duration = 0` 说明**时长也没传上来**，与「浏览器跑的还是旧版前端」吻合
> （`durationSec` 是 P26 才加进上报的）。但**旧前端不该是理由** —— 后端必须自己兜住，
> 这正是本次改造的重点：**任何情况下都不允许写出半截数据**。

## 3 · 修复

### 3.1 判定统一到 `media_id`（唯一凭据）

`toSection()` 改为：

```java
Long mediaId = row.getMediaId();
if (mediaId != null) {
    s.getVideo().setStatus("done");       // 有媒资才算「已配视频」
    s.getVideo().setMediaId(mediaId);     // 回传前端，目录往返时不丢
    ...
} else if (videoName 非空) {
    log.warn("小节 {} 只有文件名没有媒资，按「未配视频」处理", ...);   // 不谎报
}
```

顺带把 `mediaId` 加进草稿接口的 `video` 对象 —— 前端 `persistCatalog()` 是**原样回传**这份数据的，
不带上就会在往返中丢失（实测已验证往返后不丢）。

### 3.2 登记改为「严格校验 + 自愈」

```
① payload 里的 mediaId → 必须能被媒资库确认（GET /media-meta/by-id）
   不存在 → 丢弃并 warn
② 仍无 mediaId → 按文件名反查（GET /media-meta/latest-by-filename）
③ 还拿不到 → 400「视频未完成上传，请重新上传该小节的视频」——**宁可失败，不写半截数据**
④ 时长以**媒资记录的真实值**为准（前端预读失败时为 0，这里兜住）
⑤ 清除视频时：`video_name` 与 `media_id` **同时清**（只清一个又会造出半截数据）
```

媒资侧新增两个内部端点（**刻意放在 `/media-meta/**`，避开 `/medias/**` 的登录拦截**）：

| 端点 | 用途 |
|---|---|
| `GET /media-meta/by-id?id=` | 校验前端带来的 mediaId 真实存在 |
| `GET /media-meta/latest-by-filename?filename=` | 兜底反查 + 取真实时长（返回 `{id, duration}`） |

DTO `MediaMetaDTO` 放在 `tj-api`（`tj-media` 已依赖它，不会成环）。

## 4 · 实测

| 场景 | 结果 |
|---|---|
| 反查接口 | ✅ `{"id":"10","duration":12.0}` |
| 草稿接口 `video` 字段 | ✅ 带 `mediaId`，且 2.1 判定为 `done` |
| **登记时不传 mediaId、不传时长** | ✅ `media_id` 自动补、**时长自动取媒资的 12 秒**（不再是 0） |
| **登记时传不存在的 mediaId + 不存在的文件名** | ✅ **HTTP 400**「视频未完成上传…」，库里**无任何写入** |
| 传假 mediaId 但文件名真实 | ✅ 自动回退按文件名反查成功（自愈） |
| 服务日志 | ✅ 两条 warn 如实记录（不静默） |
| **目录保存往返**（模拟改目录） | ✅ 5 个小节的 `mediaId` 全部保留、小节数 11 不变 |
| 2.1 学生端播放凭证 | ✅ 返回 `mediaUrl` |
| 2.1 视频流 | ✅ 206 `video/mp4` 1024B，文件头是合法 mp4 |

**数据修复**：2.1 挂上它对应的媒资（`media_id=9`、时长按媒资记录取 **148 秒**），草稿表 + 正式表都补。
全库扫描确认 1002 课程再无「有文件名、没媒资」的行。

> 扫描时发现**其它课程**也有类似行（多为种子数据形态：`video_name` 直接等于小节名，如"核心概念与快速上手"）。
> **未改动它们** —— 不属于本次范围，动别人的数据风险大。

## 5 · 你只需要

**强刷**（`Ctrl+Shift+R`）后，学生端进 2.1 应该能播（我已经把数据补齐，不必重传）。

然后建议**再走一遍完整流程**验证：在**任意没有视频的小节**上传一次 → 保存上架 → 学生端看。
如果这中间任何一步失败，现在会**明确弹错误提示**，不会再有"看起来成功了但学生端没有"。

## 6 · 遗留

- 其它课程里的半截数据（种子数据形态）未清理 —— 需要时再说。
- 前端 `courseWizard` 的 `s.video` 本地状态与后端返回的同构，已一致；无前端改动。
