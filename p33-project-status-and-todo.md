# P33 · 项目现状盘点 · 待办清单 · 简历向总结

> 2026-09-18 · 用户问「还有哪些待做」。**本清单是实地扫描出来的，不是凭记忆列的** ——
> 上一轮 P31 的教训（旧结论不能当依据）在这里同样适用，所以这次所有数字都来自当前代码与数据库。
>
> 扫描口径：`git` 工作区当前代码 + 运行中的 23 个容器 + MySQL 实际数据。

---

## 一、项目规模（可直接写进简历的数字）

| 维度 | 数值 |
|---|---|
| 后端 | **10 个微服务** · 872 个 Java 文件 · **52,094 行** · **252 个 HTTP 端点** |
| 前端 | **161 个 Vue 组件** · 55,921 行（vue + js）· 单个 SPA 承载双端 |
| 学生端 | **21 条路由，全部为真页面**（零占位） |
| 讲师端 | **15 条路由，全部为真页面**（零占位） |
| 运行环境 | **23 个容器**（15 个服务 + 8 个基础设施：Nacos/网关/MySQL/Redis/RabbitMQ/ES/pgvector/nginx） |
| 数据库 | 4 个业务库（`tj_course` / `tj_exam` / `tj_user` / `tj_learning`）+ `tj_media`；**7 份 SQL 迁移** |
| 过程资产 | **34 份任务复盘报告**（`p*.md`）+ 长期记忆 + 细节坑库 |

**端点分布**：course 47 / learning 52 / exam 39 / ai 27 / trade 27 / user 22 / media 14 / search 14 / promotion 8 / remark 2

---

## 二、已交付的能力（按端）

### 学生端（21 页）
首页 / 课程列表 / 课程详情 / **学习页**（目录·问答·笔记·AI助教·考试五页签）/ 考试答题 / 考后解析 /
学习记录（**学习足迹热力图**·累计时长·连续打卡）/ 消息 / 通知 / 笔记 / 我的积分 / 我的资料 /
购物车 / 优惠券 / 结算 / 支付 / 支付成功

### 讲师端（15 页）
工作概览 / 我的课程 / **建课向导**（4 步：基本信息→目录→视频→讲师）/ 题库管理 / 录题 / 题目可见范围 /
考试管理 / 建卷向导 / **试卷批改**（复核流）/ 考试统计 / 批改工作台 / **学生分析** / 个人资料 / 师生对话

### 关键链路（都能端到端跑通并实测过）
1. **视频**：本地上传（真进度）→ 媒资登记 → 保存上架 → 学生端直链播放（Range 分段、拖进度、续播）
2. **考试**：组卷 → 发布 → 作答（**判分读冻结快照**）→ 交卷 → 讲师复核 → 统计
3. **学习行为**：播放埋点 → 完成判定（**真实时长 70%**）→ 课表进度 → 学习时长按天聚合 → 热力图
4. **题库**：公共池 + 私有工作区（引用式共享，撤回被引用的题会被拦并报出试卷名）
5. **师生对话**：WebSocket

---

## 三、待办清单（按建议优先级）

### 🔴 第一档：小而快、演示可见（建议先做，半天量级）

| # | 事项 | 现状 | 说明 |
|---|---|---|---|
| 1 | **「教师」→「讲师」文案** | 前端模板 **6 处**；后端字符串 23 处（多数是 Swagger 文档注释，真正用户可见的是错误提示如「请至少设置一名教师」和角色名） | ⚠️ **旧记录说的"49/44 处"是高估** —— 那是把注释、代码标识符一起数了。按用户可见口径，实际**只剩十余处** |
| 2 | **清理死文件** | `pages/teacher/Placeholder.vue`、`pages/student/components/ComingSoon.vue` **均已无路由使用**（只剩注释和一句未使用的 import）；`pages/main/index.css` + `index.min.css` 待确认引用 | 双端已"零占位"，这两个占位组件是历史遗留 |
| 3 | **教师端补路由守卫** | 当前**没有全局守卫** → 无 token 也能渲染页面（接口才 401） | 演示时容易被误认为"没登录也能进后台" |
| 4 | **上架/上传的进行中反馈** | 只有按钮文字变"提交中…" | 大课或网络慢时缺一个进行中遮罩（上轮提过，待你定） |

### 🟠 第二档：功能缺口（后端未实现，用户能感知）

| # | 事项 | 现状与依赖 |
|---|---|---|
| 5 | **题库 Excel 批量导入** | 前端一处 `Promise.reject('后端尚未实现')`；**后端无接口**。讲师目前只能逐题录入 —— 这是讲师侧最大的效率痛点 |
| 6 | **题目 ↔ 知识点关联** | 知识点是**课程级资产**，但`knowledgePoints` 恒空 → 讲师端相关页面显示「—」 |
| 7 | **讲师端「学生分析」补学习时长列** | **数据源已具备**（P27 的 `learning_duration`），只差接口带一个字段 + 前端一列 |
| 8 | **逐小节学习明细** | `learning_record` 结构与数据都在（现 6 行），但"学到第几节 / 逐节完成情况"没有做成视图 |
| 9 | **题库使用信号 + 众包纠错** | 公共池的**质量机制**：引用数 / 被考次数 / 正确率，以及用户报错题。目前公共池无任何质量信号 |
| 10 | **定时发布** | `course` 表**没有"定时公开时间"字段**（只有 `publish_time` = 实际上架时间）→ 需要加字段 + 定时任务 |

### 🟡 第三档：工程债 / 数据清理

| # | 事项 | 说明 |
|---|---|---|
| 11 | **媒资文件无自动清理** | 反复上传会堆积（已清理过两轮：876 MB + 532 MB）。建议加"删除小节视频时顺带标记孤er文件"或定期清理任务 |
| 12 | **其它课程的半截媒资数据** | 首轮扫描发现若干「有文件名、无媒资」的历史行（多为种子数据形态，`video_name` 直接等于小节名）→ 未动 |
| 13 | **热力图无历史数据** | 埋点是 P27 才加的，**之前的学习没有时长记录** → 格子从那天起才亮 |
| 14 | `queryMarkdownPage` 参数名 | 应为 `params`（旧站遗留） |

### ⚪ 已知限制（是范围决定，不是缺陷）

- **媒体流不走网关** —— 网关转发 Range 请求是坏的（返回 206 + 89B JSON），所以前端直连媒体服务端口；生产部署把 `VITE_MEDIA_BASE_URL` 指向媒体服务域名/独立反代即可
- **AI 未用 RAG** —— 学习工具走「全量资料」上下文
- **演示范围** —— 课程 AI 匹配任意课程；知识库限「IT 互联网」24 门
- **「怎么成为讲师用户」** —— 用户明确说先不管
- **无视频小节不做标记** —— 用户明确说不需要

---

## 四、简历向总结（可直接用的表述）

### 项目描述（一段式）

> **知序学堂** · 面向个人的在线教育创作平台（讲师自主建课 + 学员学习闭环）
> 独立负责全栈：10 个 Spring Cloud 微服务的接口设计与实现、双端 SPA（学员端 21 页 / 讲师端 15 页）的
> 交互与视觉落地，覆盖建课、题库、考试、判分、学习行为追踪、视频媒资与师生实时通讯全链路。
> 后端 5.2 万行 / 前端 5.6 万行 / 252 个端点 / 23 个容器编排运行。

### 技术栈

`Spring Boot` `Spring Cloud Alibaba（Nacos）` `Spring Cloud Gateway` `MyBatis-Plus` `MySQL` `Redis`
`RabbitMQ` `Elasticsearch` `pgvector` `Docker Compose` `Vue 3` `Element Plus` `SCSS` `Vite`

### 可以讲的 5 个技术难点（都是真实解决的）

1. **复活一个"从未成功构建过"的服务**
   媒资服务长期崩溃重启，表象是"缺云存储凭证"。深挖发现根因是**该模块漏声明编译期依赖** → 全模块注解生成的
   方法都不存在 → **本仓库代码根本编译不出来**，容器里跑的是来源不明的旧产物。
   修复依赖后重写构建，并**新增本地磁盘存储实现 + 解开三处对云 SDK 的硬依赖**，服务首次以本仓库代码稳定运行。

2. **定位一个"改了却看不到"的隐蔽同步缺陷**
   讲师替换/删除视频后学员端始终显示旧视频。逐层排除前端与缓存后，定位到上架同步用的
   `INSERT ... ON DUPLICATE KEY UPDATE` **更新列表漏了媒资字段** —— 行已存在时只更新列表内的列，
   漏掉的字段**永远保持旧值且不报错**。补齐字段列表并建立"新增字段必须两处同加"的约束。

3. **视频链路本地化与网关缺陷规避**
   实现上传（真进度）、媒资登记、直链播放（HTTP Range 分段）。发现**网关转发 Range 请求被破坏**
   （返回 206 + 89B JSON），改由前端直连媒体服务并做成可配置，规避了改网关的高风险。

4. **学习行为埋点与防刷口径设计**
   完成判定以**播放器真实时长**（而非课程预设值）为基准取 70%；学习时长按"播放坐标推进量 ∩ 单次上限"
   计量 —— 拖进度条与挂机都不产生时长，既不过度采集又能反映真实投入；时长按 `用户+课程+日期` 唯一聚合并支持热力图。

5. **鉴权加固与越权收口**
   修复一处**试卷答案泄漏**（接口未按作者过滤），补全讲师侧接口的**归属校验并下沉到服务层**（不依赖前端拦截），
   网关无条件剥离客户端伪造的身份头。

### 工程习惯（面试可聊）

单一数据源（所有聚合数字从明细推导）、**不编造接口没有的字段**（无数据位置保留版式显示「—」）、
正常业务状态与数据异常文案分离、每个任务留存复盘报告（34 份）并沉淀为长期记忆与坑库。

---

## 五、建议的下一步

**如果目标是"拿得出手的演示"** → 先做第一档 1–3（文案 + 清死文件 + 路由守卫），都是收尾性质、见效快。

**如果目标是"功能完整度"** → 做第二档第 5 项（Excel 批量导入），它是讲师侧唯一还卡着的效率瓶颈。

**如果目标是"面试可讲深度"** → 第二档第 9 项（题库使用信号 + 众包纠错）最有内容：涉及数据模型设计、
反爬/防刷、以及"公共资源如何自净"的产品思考。
