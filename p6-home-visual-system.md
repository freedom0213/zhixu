# P6 · 首页视觉体系升级（清透 / 轻玻璃 / 空间层次）

> 目标：把首页从「白色课程管理平台」升级为「清透、柔和、有空间感、具备轻玻璃质感的现代学习空间」。
> 核心原则：**不是增加颜色，而是增加视觉层次**。全站仍只用 1 个主色 + 1 个辅助色。

---

## 一、设计系统：四层结构（已写入 `theme.scss`）

| 层 | 角色 | 实现 | 面积占比 |
|---|---|---|---|
| **L3 强调层** | CTA / 进度 / 价格 / 徽标 | 主色 `--color-main` | < 5% |
| **L2 内容层** | 卡片 | 白底 + **发丝线** + 双层柔和阴影 | ~20% |
| **L1 玻璃层** | 导航 / 悬浮控件 / 强调容器 / 抽屉 | `--glass-*` + `backdrop-filter` | ~10% |
| **L0 背景层** | 整站底衬 | 浅蓝灰渐变 + 2 处极克制柔光晕 | ~70% |

### 新增 design token

```scss
// L0
--page-bg-from: #F8FAFD;   --page-bg-to: #EDF2F9;
--page-glow-a: rgba(96, 140, 255, 0.11);   --page-glow-b: rgba(45, 190, 178, 0.07);
// L1
--glass-bg: rgba(255,255,255,0.62);   --glass-bg-strong: rgba(255,255,255,0.78);
--glass-line: rgba(255,255,255,0.70); --glass-blur: 18px;
// L2
--card-line: rgba(16,24,40,0.055);    --card-line-hover: rgba(16,24,40,0.10);
--hairline: inset 0 0 0 1px var(--card-line);
--hairline-hover: inset 0 0 0 1px var(--card-line-hover);
// 节奏
--space-module: 56px;   --space-module-sm: 40px;
```

### 两个关键技术决策

**① 背景层写在 `html` 上，而不是 `position: fixed; z-index: -1` 的独立层。**
CSS 绘制顺序里，负 z-index 的子元素绘制在「建立层叠上下文的元素之背景」之上、却在**在流块级后代（`body`）的背景之下**。
所以只要 `html` 有自己的背景（本项目原本 `html,body` 同一条规则都设了底色），body 的底色就会把负 z 子层整个盖住。
改法：`html` 承载渐变（`background-attachment: fixed` 保持柔光不随滚动漂移），`body` 设为 `transparent`。

**② 卡片用 `inset` 发丝线替代 `1px` 实边框。**
- 视觉上远比实边框柔和 —— 这是消除「传统后台系统硬框感」的关键；
- `inset` 阴影**不参与布局计算**，因此去掉边框不会造成任何像素位移（全局 `box-sizing: border-box`）。

### 玻璃层的两条底线

- **只给「浮在内容之上」的元素用**：内容密集的卡片一律不用（保证文字可读性 + 避免每个 `backdrop-filter` 都产生一次离屏合成）。
- **必须带实色兜底**：`@supports not ((backdrop-filter: blur(4px)) or (-webkit-backdrop-filter: blur(4px)))` 下退化为 `rgba(255,255,255,0.94)`，否则半透明底会让背后内容透出来、文字读不清。

---

## 二、页面层级与节奏重构

### 改造前

```
欢迎卡（左栏内，856px 宽）
↓
分类+轮播 312px
↓
兴趣
↓
直播公开课（白底带）→ 精品好课 → 推荐课程   每模块 10 卡 / 3 列 / 4 行 / 1261px
右边：5 张卡挤在 1208px 内，其下留 3177px 空白
```

### 改造后

```
Hero 欢迎区（全宽 1200，L1 玻璃卡）      ← 首屏第一视觉
↓ 56px
两栏：左 = 分类导航 + 轮播(降权) → 兴趣模块
      右 = 学习状态（打卡 / 今日计划 / 最近学习）
↓ 56px
课程区（全宽 4 列）直播公开课 → 精品好课 → 推荐课程
↓ 56px
次要信息带：学术新闻 ｜ 领优惠券（各 588px）
```

三处结构性决定：

1. **Hero 从"左栏内的一张卡"提升为全宽首屏区块** —— 原来它被限制在 856px 的窄列里，没有资格当第一视觉。
2. **课程区改为全宽 4 列** —— 卡片宽度 271 → 285px，10 张卡从 4 行减到 3 行，单模块高度 1261 → 949px。
3. **新闻 / 优惠券迁到页面底部「次要信息带」** —— 它们原来压在右侧栏底部，
   而右侧栏下方还有 **3177px 空白**；迁移后右栏只放"学习状态"，与左栏高度基本对齐，空白归零。

---

## 三、顺带修复的既有缺陷

**一级分类溢出白框 78px（真实存在的破版）**

- 一级分类共 **5 个**（101 IT互联网 / 102 设计创意 / 103 办公职场 / 104 语言学习 / 105 考试认证），实测每个 `height: 78px` → 合计 **390px**；
- 但白色分类框写死 `height: 312px`，且无 `overflow: hidden` →
  第 5 项「考试认证」**溢出到白框外 78px，直接压在下方轮播区上**（实测：最后一项 bottom=683 / 白框 bottom=605）；
- 修法：分类项 `78 → 64px`（`padding` 15 → 10px），并让白框 `height: auto` 跟随内容
  （`ClassCategory` 自带 `.items { min-height: 312px }` 兜底，白框永远不会矮于幻灯片）。
- 修复后实测：`5 × 64 = 320 vs 白框 320 => 溢出 0px`。

---

## 四、实测数据（无头 Chrome，同源 iframe，非推断）

视口 1440×900（版心 1200）：

| 指标 | 改造前 | 改造后 |
|---|---|---|
| 页面背景 | `#F5F7FA` 纯平灰 | 浅蓝灰渐变 + 2 处柔光晕（L0 生效，已读计算样式确认） |
| `backdrop-filter` 使用处 | **0** | Hero 玻璃卡：`saturate(1.6) blur(18px)` + `rgba(255,255,255,0.78)` |
| 卡片边界 | `1px solid #F0F1F3` | `inset 0 0 0 1px rgba(16,24,40,.055)`（border-top-width = **0px**） |
| 一级分类溢出白框 | **+78px**（破版） | **0px** |
| 幻灯片高度 | 312px | **208px**（−33%） |
| 课程卡 | 3 列 / 856 列宽 / 卡宽 271px | **4 列 / 1200 全宽 / 卡宽 285px** |
| 单课程模块高度 | 1261px | **949px**（−25%） |
| 右侧栏下方空白 | ~3177px | **0** |
| 首页总高 | 4657px | 4413px |
| 模块间距 | 30px（各模块自带 `padding-top`） | **56px**（统一由 flex `gap` 控制） |

### 跨页回归（改的是全站设计系统，必须回归）

| 页面 | 结果 |
|---|---|
| `/search/index` | 200，12 张 `.soft-card` 全部升级为「无边框 + 发丝线」 |
| `/main/news` | 200，12 张 `.soft-card` 同上 |
| `/main/ai` | **`OUTER_OVERFLOW = 0`**（AI 页"占满视口、内部滚动"的强约束未被破坏），标题「私人 AI 助手」正常 |

---

## 五、按内容密度做差异化（你的要求 1）

- **统一的部分**（全站一致）：圆角体系、双层柔和阴影、发丝线替代硬边框、背景层次逻辑、`.glass` 基类。
- **首页更强**：唯一使用真实玻璃的元素（Hero）、最大模块间距（56px）、全宽 4 列课程栅格、可见的浅蓝灰渐变底。
- **密集页面适当弱化**：搜索 / 新闻页保持同一套卡片语言，但不加玻璃、不放大留白
  —— 它们内容密度高，加大留白只会让单屏信息量骤降。

---

## 六、明确**没有**做的事（避免误解）

1. **没有新增任何颜色**。全站仍是主色 `#3B82F6` + 辅助色 `#14B8A6`，L3 强调色只出现在 CTA / 进度 / 价格。
2. **没有改接口、没有改数据结构、没有动后端**。所有模块用的仍是原来的接口
   （`/class/recommendClassList`、`/class/myLearning`、`/news/list` 等）。
3. **没有删减课程数量**。三个课程模块各 10 条是接口返回的真实数据，全部保留（只改排布）。
   若你希望首页更轻，可以把每模块收敛到 8 条（2 行 × 4 列），这需要你确认——它会隐藏课程。
4. **没有让 AI 悬浮助手变玻璃**。它的面板内部是消息列表（`#f7f8fa` 实底），
   改成透明会直接损害聊天文字的可读性 —— 这正符合你「不要让所有组件都变成透明玻璃」的要求。
5. **AI 对话页的底色 `--ai-canvas` 未改**，仍是不透明的自有画布，因此不会被新背景层穿透。

---

## 七、改动文件清单

| 文件 | 改动 |
|---|---|
| `src/style/theme.scss` | 阴影改双层柔和；新增 L0–L3 四层 token（背景 / 玻璃 / 发丝线 / 节奏） |
| `src/style/index.scss` | `html` 承载背景层 + `body` 透明；`.soft-card` 升级；新增 `.glass` 基类（含 `@supports` 兜底） |
| `src/pages/main/index.vue` | 结构重排：Hero 全宽 / 两栏 / 课程区全宽 / 次要信息带；新增 `recentPercent` 进度环与 `goLearn` |
| `src/pages/main/index.scss` | 重写：56px 模块节奏、Hero 玻璃卡与进度环、Banner 降权、分类项紧凑化、次要信息带栅格 |
| `src/components/ClassCards.vue` | 去硬边框 + 双层阴影 + 封面 hover 轻微放大（搜索页共用，一并升级） |
| `src/pages/main/components/sidebar/NewsCard.vue` | 适配 588px 宽位：自适应多列 + 用留白/hover 底色替代分割线 |
| `src/pages/main/components/sidebar/CouponCard.vue` | 适配宽位：自适应多列；暖色语义保留，硬边框换发丝线 |

---

## 八、需要你在登录状态下确认的两处

无头 Chrome 用的是全新会话（本站 token 存在 `sessionStorage`，无法复用你浏览器里的登录态），
所以下面两处在"未登录"量测中不渲染，**我无法用截图/几何数据验证**，请你用 `123456` 账号看一眼：

1. **Hero 进度环**（仅在 `courseId` + `sections` 都有值时才渲染）——
   用 `conic-gradient` 画，百分比取 `learnedSections / sections`。已确认该 CSS 正常编译；
   不渲染说明该接口没返回学习记录，属预期。
2. **兴趣模块在左栏的位置** —— 它由 `interest.size > 0` 控制，未登录时不显示。
   登录且已设置兴趣时，左栏 = 轮播 320 + 40 + 兴趣(约 385) ≈ 745px，
   与右侧栏高度基本对齐；未设置兴趣时左栏较矮，其下会留约 200px 空白（属"呼吸感"留白）。

---

## 九、环境约定（沿用）

dev server 必须显式指定端口，**不要用 `npm run dev`**（配置里 `server.port=18082` 会静默撞上 Docker 容器）：

```bash
npx vite --port 18090 --mode development
```

容器演示环境 `:18082` 仍是上一版（P5）的构建产物；本轮改动只在 `:18090` 可见。
若要让 `:18082` 同步，需要 `docker compose build frontend` + `up -d --no-deps frontend`（会动运行环境，需你确认）。
