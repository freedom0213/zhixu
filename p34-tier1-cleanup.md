# P34 · 第一档收尾：文案统一 / 清死文件 / 讲师端守卫 / 提交中反馈

> 2026-09-18 · 按 P33 盘点的第一档执行。第二、三档用户明确「暂不做」。

---

## 1 · 文案：「教师」→「讲师」（实测只有十余处，不是 49 处）

**先修正一个被高估的旧结论**：之前记的"约 49 处/44 处待替换"，是**把注释、Swagger 文档、代码标识符一起数了**。
按「**用户可见**」口径重新扫描，实际只剩这些：

### 前端（6 处）
| 文件 | 原文案 | 改为 |
|---|---|---|
| `pages/teacher/TeacherLayout.vue` | `brand-name="教师工作台"` | **讲师工作台**（最显眼的一处） |
| 同上 | `nav-label="教师工作台主导航"` | 讲师工作台主导航（读屏软件会念） |
| `pages/teacher/courses.vue` | 让管理员把你的**教师身份**关联到该课程 | 讲师身份 |
| `pages/personal/myExamDetails.vue` | **教师评语**： | 讲师评语 |
| `pages/footer/how-to-select-courses.vue` | 查看课程介绍、教学大纲、**教师信息**等详情 | 讲师信息 |
| `components/shell/TeacherTopBar.vue` | 模板注释里的「教师端」 | 讲师端（注释） |

### 后端（7 处）
| 位置 | 类型 | 说明 |
|---|---|---|
| `CourseErrorInfo` 「请至少设置一名教师」 | **用户可见** | 上架前校验不通过时会弹出来 |
| `SearchErrorInfo` 「教师信息不存在」 | **用户可见** | 接口错误提示 |
| `UserController` 「…不是教师身份的账号，不能作为讲师」 | **用户可见** | 加协作讲师失败时的提示（顺带修掉了原来"教师…作为讲师"的自相矛盾措辞） |
| `IExamService` 注释 / `InteractionQuestionServiceImpl` 注释 | 注释 | 口径统一 |
| `RoleDTO` / `UserDetailVO` 的 Swagger `example="教师"` | 接口文档 | 会误导对接方，一并改 |
| `UserDTO` 的「老师和学生不用填」 | 接口文档 | 「老师」也统一为「讲师」 |

**没动的**：`/teacher/*` 路由、`TeacherController`、`teacherApi.js` 等**代码标识符**（用户 9-15 明确定过：只改用户可见文案，标识符不改名）。

**验证方式**：把运行中容器的 jar 取出来逐 class 扫描 ——
三个服务**旧文案零残留、新文案已生效**（比只看源码更可靠，因为它验证的是真正在跑的那份产物）。

---

## 2 · 清死文件

双端导航已全部实装，两个占位组件**再无任何路由引用**（只剩注释里提了一句）：

| 文件 | 状态 |
|---|---|
| `pages/teacher/Placeholder.vue` | 删除（`teacher.js` 里那句未使用的 `const Placeholder = …` 一并清掉） |
| `pages/student/components/ComingSoon.vue` | 删除（`student.js` 里本来就只 import 过外壳，没 import 它） |

- 两个路由模块的顶部注释同步更新为「导航已全部实装，站内零占位」。
- ⚠️ 删除前**先备份**到 `.workbuddy/removed-placeholders/`（`Placeholder.vue` 属未跟踪文件，删了 git 也找不回；备份是零成本保险）。
- 删后全量 grep 确认无残留 import，vite 编译通过。

> 顺带说明：`pages/main/index.css` / `index.min.css` 确认无引用，但它们属**老站**文件，
> 不在本次范围（老站可能被其它入口引用），**未动**。

---

## 3 · 讲师端登录守卫

**原先两端都没有守卫** → 未登录也能把整个后台界面渲染出来（只是接口 401），观感上就是"不用登录就能进后台"。

在 `router/index.js` 加了一个 `beforeEach`：

```
访问 /teacher/* 且 sessionStorage 里没有 token
  → 记下来源（复用 config/loginRedirect 里既有的机制）
  → 跳 /login
登录成功后 → 回到原来要去的那个讲师页（该机制本来就支持，不用新写）
```

**两个刻意的取舍**：
1. **只校验"有没有登录"，不做角色校验** —— 管理员和讲师都能进，避免误伤；"这门课是不是我的"由**服务端的归属校验**兜底（P19/P24 已下沉到服务层）。前端拦角色不可靠，也不该是唯一防线。
2. **学员端不拦** —— 课程列表 / 详情本来就是公开内容，匿名浏览是产品设计。只给后台加锁。

**双向验证**（同源 iframe 探针，注入 sessionStorage 前后对比）：

| 场景 | 结果 |
|---|---|
| 无登录态访问 `/teacher/dashboard` | ✅ 跳 `#/login`，渲染登录页 |
| 有登录态访问同一地址 | ✅ 停在 `#/teacher/dashboard`，渲染出「讲师工作台」 |

> ⚠️ 这个改动**改变了以后的调试方式**：以前"无 token 也能渲染讲师端页面"，现在必须注入 `sessionStorage.token` 才能无头验证讲师端。已同步进项目记忆。

---

## 4 · 提交上架的进行中反馈

`publish()` 期间显示一层轻遮罩（半透明白 + 毛玻璃 + 卡片内 spinner + 文案「正在提交上架…」/「正在保存草稿…」），
同时挡住重复点击。

**两个细节**：
1. **最短展示 500ms** —— 上架是本地事务、很快就返回，但**太快反而让人看不见**，所以即使秒回也保证提示可见半秒，再切到结果卡。
2. **`z-index` 取 69（低于结果卡的 70）** —— 两者不同时出现，但万一同时在场，也让"结果"压在上面，不会被遮罩盖住。

---

## 5 · 验证汇总

| 项 | 结果 |
|---|---|
| 前端 SFC 校验（6 个改动文件） | ✅ 0 错误 |
| 前端 vite 编译 | ✅ 全部 200 |
| 路由/配置文件 JS 语法 | ✅ 3 个文件通过 |
| 后端编译（course/search/user/exam/learning） | ✅ BUILD SUCCESS |
| 后端产物文案 | ✅ 三服务旧文案零残留、新文案已生效 |
| 守卫双向行为 | ✅ 见上表 |
| 删除残留引用 | ✅ 无（仅注释提及） |

> 后端改动的 5 个模块中，`exam` / `learning` **仅改注释**（不影响运行），未重建容器 —— 下次构建会自然带上。
> 真正需要生效的 `course` / `search` / `user` 三个服务已重建并验证。

---

## 6 · 未做（用户明确"暂不做"）

第二档：Excel 题库导入 / 知识点关联 / 学生分析补时长列 / 逐小节明细 / 题库使用信号 / 定时发布。
第三档：媒资自动清理 / 其它课程半截数据 / 热力图历史 / `queryMarkdownPage` 参数名。

详见 `p33-project-status-and-todo.md`。
