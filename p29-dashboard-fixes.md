# P29 · 首页统计为空 / 签到状态 / 冗余视频清理

> 2026-09-18 · 用户强刷后反馈「学习足迹还是不显示、累计学习和笔记还是空」。
> **我上一轮的两个判断是错的**，这里如实记录。

## ❌ 我上一轮的错误判断

| 我说的 | 实际 |
|---|---|
| 「累计学习/笔记为空是**页面没强刷**」 | **是我的代码 bug**：少取了一层响应包装 `.data`，`totalSec` 恒为 `undefined` → 永远显示「—」 |
| 「签到功能没问题」 | **不准确**：解析 bug 确实存在（把标记数组当对象），而用户描述的"首页总显示已打卡、得去积分页才真打卡"**正是它导致的** |

## 1 · 真正的根因：少取了一层响应包装

`src/utils/request.js` 的响应拦截器返回的是**网关包装体**：

```js
if (code === CODE.REQUEST_SUCCESS) {
  return response.data;      // ← 这是 { code, msg, data }，不是业务数据本体
}
```

而我 P27 写的 `loadDuration` 直接读 `d.totalSec`：

```js
const d = await getLearningDurations(...);
durationTotal.value = d?.totalSec ?? null;   // ← d 是包装体，totalSec 永远 undefined → null → 「—」
```

**连带影响**：`durationMap` 恒为空 → **热力图所有格子都是 lv0（空白）** —— 这才是"学习足迹不显示"的真因
（不是我上一轮猜的"分档下限"；当然那个也确实该改，已改）。

**修复**：统一先解包（兼容裸返回）：

```js
const d = res && typeof res === 'object' && 'code' in res ? (res.code === 200 ? res.data : null) : res;
```

`dashboard.vue` / `records.vue` 两处都改了。

> ⚠️ 这是本项目的高频坑：**服务层返回裸 JSON，但网关会包成 `{code,msg,data}`**，
> 前端拿到的始终是包装体 —— 每个新写的接口读取处都要记住取 `.data`。

## 2 · 学习笔记为空：接口用法本身就错了

`loadNotes()` 用 `getAllNotes({ pageNo: 1, pageSize: 1 })` 打 `/ls/notes/page`，
而这个接口**必须带课程/小节**：

```
GET /ls/notes/page?pageNo=1&pageSize=1  →  {"code":400,"msg":"课程或小节不能为空"}
```

所以它从来就不可能返回"我的笔记总数"。

**修复**：新增专用接口
- 后端 `GET /ls/notes/my-count` → 我的笔记总数（只算自己的、未被隐藏的）
- 前端 `countMyNotes()` 改用它 ✅ 实测返回 `3`（库里正好 3 条笔记）

## 3 · 签到：首页那个胶囊本来就是打卡按钮

- 首页右上角确实**已有打卡按钮**（`doSign`），文案 `今日打卡 / 打卡中 / 已打卡`
- 它一直显示"已打卡"是因为**解析 bug 把每天都算成了"今天已打卡"** → 按钮被 `disabled` 掉 → 用户看着像"不能点，得去积分页打"
- 解析修复后（`utils/signMarks.js`），**今天没打卡时会显示"今日打卡"且可点** ✅

> 结论：不需要新增打卡功能，修掉状态误判即可。你之前每天看到"已打卡"而实际没打，就是这个 bug 的表现。

## 4 · 冗余视频清理（已执行，不可恢复）

实际是 **3 份**重复（不是 2 份），其中一份连数据库记录都丢了（孤儿文件）：

| media id | 文件名 | 大小 | 引用 | 处置 |
|---|---|---|---|---|
| 3 | demo-video.mp4 | 3 MB | **1.1 在用** | 保留 |
| 4 | 崩坏：星穹铁道…mp4 | 59 MB | **1.2 在用** | 保留 |
| 5 | Minecraft…mp4 | 292 MB | 无 | **已删**（孤儿，无记录） |
| 6 | Minecraft…mp4 | 292 MB | 无 | **已删** |
| 7 | Minecraft…mp4 | 292 MB | 无 | **已删** |
| 8 | Minecraft…mp4 | 292 MB | **1.3 在用** | 保留 |

- 删除前二次确认引用数 = 0 ✅
- 结果：卷 `media-data` 从 **~1.2 GB → 354 MB**（释放约 **876 MB**）
- 删除后复验：1.1 / 1.2 / 1.3 三个视频仍返回 `206 video/mp4` ✅

## 5 · 顺带：一次"假报错"

重新编译时出现一批 `找不到符号` / `未覆盖抽象方法 getDesc()` 的报错，但对应源码与 import 都是好的 ——
是**增量编译产物不一致**。`clean package` 后一次通过。**这类报错先 clean 再判断。**

## 6 · 实测

| 场景 | 结果 |
|---|---|
| `GET /ls/notes/my-count`（经网关） | ✅ `{"code":200,"data":3}` |
| `GET /ls/learning-durations/summary`（经网关） | ✅ `totalSec: 293`（今日 224 秒） |
| 1.1 / 1.2 / 1.3 视频流 | ✅ 均 `206 video/mp4` |
| 前端两页 vite 编译 | ✅ 200 |

## 7 · 需要你做的

**强刷页面**（`Ctrl+Shift+R`）后：
1. 「累计学习」应显示具体时长（你今天已学 3 分多钟）
2. 「学习笔记」应显示「3 篇」
3. 学习足迹今天那格应亮起
4. 若今天还没打卡：右上角应显示**「今日打卡」且可点**（点了才会变成"已打卡"）
