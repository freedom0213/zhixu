# P37 · 两处界面修正：考试时间改成日期选择器 / 摘掉知识库「编辑」

> 2026-09-18 · 用户报：① 新建考试步骤③的开始/结束时间要**手打**，希望能**选日期**；② 知识库「编辑」保存必失败（选 A 方案：摘掉入口）。

---

## 1 · 新建考试步骤③：时间从「手打文本框」改为「日期时间选择器」

**改动位置**：`tj-front/tj-protal/src/pages/teacher/examWizard.vue`（发布设置区）

**做法上的一个选择**：这个页面**完全不用 Element Plus**（用的是项目自己的 `w-input` / `w-select` / `q-btn` 设计系统），
所以**没有引入 `el-date-picker`**，而是用浏览器原生 `<input type="datetime-local">` ——
点右侧图标即出日历 + 时分，**零新依赖、与现有样式天然一致**。

**一个必须处理的细节**：原生控件要求的值格式是 `YYYY-MM-DDTHH:mm`（**带 T**），
而表单与接口用的是 `YYYY-MM-DD HH:mm`。所以在「进出控件」这一层做了两个纯格式搬运函数，
`form.startAt` / `form.endAt` 的**数据形状完全不变**，下游的接口适配层与提交逻辑一行都不用改：

```js
const toPickerValue   = (v) => (v ? String(v).replace(' ', 'T').slice(0, 16) : '');
const fromPickerValue = (v) => (v ? String(v).replace('T', ' ').slice(0, 16) : '');
```

**顺带补的两件事**（原逻辑没管时间顺序）：
1. **时间窗顺序校验** —— `结束时间 <= 开始时间` 时给出提示。因为格式零填充，**字符串比较即等价于时间比较**，不必建 `Date` 对象。
2. **顺序不对时禁用发布按钮** —— 否则会发出去一场**谁也进不去的考试**。

### ⚠️ 过程中自己踩的一个坑（值得记）

第一版把顺序校验**单独加了一条**提示，结果界面上同时出现：

```
考试时间已设置（2026-09-20 09:00 至 2026-09-20 08:00）   通过     ← 绿
结束时间早于开始时间 —— 学生进不了考场，请调整            注意     ← 橙
```

**一条说"通过"、一条说"注意"，互相打架**。这正好违反项目里那条规矩（*一条提示只承担一种语义*）。
改成**一条提示三种措辞**：未设置 / 顺序不对 / 已就绪，不再有矛盾观感。

### 验证（无头实测，不是看代码）

用同源 iframe 探针注入 `sessionStorage.token` 后直链 `#/teacher/exams/new?step=3`（该页支持 `?step=` 深链），
再模拟选择时间：

| 检查项 | 结果 |
|---|---|
| `datetime-local` 控件数量 | ✅ 2 个 |
| 尺寸 / 是否被 `.w-input` 的固定 40px 高裁切 | ✅ 335×40，`scrollHeight == clientHeight`，**未被裁切** |
| 初始态 | ✅ 「还没设置考试时间窗口」 |
| 选成 09:00 → 08:00 | ✅ 单行「结束时间早于开始时间（… → …），学生进不了考场」 |
| 改为 09:00 → 10:30 | ✅ 单行「考试时间已设置（… 至 …）」，矛盾提示消失 |

> ⚠️ **未验的一项**：`发布按钮在顺序错误时禁用` 这条逻辑没做端到端验证 —— 该按钮同时受「基本信息/选题」约束，
> 探针里这两项是空的，按钮本来就是禁用的，**无法隔离出我的改动**。属代码层改动，逻辑是单表达式。

---

## 2 · 学员端知识库：摘掉「编辑」（A 方案）

**背景**（见 `p36` §2.2）：前端 `updateMarkdown` 调 `PUT /ct/file/update`，而**后端从来没有这个端点**（实测 405）。

**先摸清入口**：编辑有**两个**入口 —— ① 列表行的「编辑」按钮；② 「查看原文」弹窗里的「编辑此文件」按钮。
**只删按钮会留下另一个入口**，所以两处都处理了。

**改了什么**（`pages/student/components/KnowledgePanel.vue`）：
- 列表行：只留「查看 / 删除」
- 查看原文弹窗：原按钮位置换成一句引导 —— 「如需修改内容：请先删除该文件，再上传修改后的版本。」
- 删掉编辑弹窗、`openEditModal` / `submitEditForm` / `editFormData` / `isEditModalVisible` / `currentEditFileId`，并清理 import
- 文件头注释写明**为何刻意不提供编辑**，避免以后有人当 bug 又加回来
- 残留检查：确认上述标识符与「编辑此文件」在文件里**零残留**

**为什么这样选**：后端缺端点的情况下，**留一个点了必报错的按钮**比"没有这个功能"更糟 ——
而且替代路径本来就有（删除后重新上传，内容与切割等级都能一并调整）。

> ⚠️ 老站还有一份同名组件 `pages/main/components/KnowledgePanel.vue`，**同样的坏按钮**。
> 它已被 `.gitignore` 排除（不随仓库走），本次**未改**；若老站入口在演示中仍会被点到，可以再处理。

---

## 3 · 验证汇总

| 项 | 结果 |
|---|---|
| SFC 校验（2 个文件） | ✅ 0 错误 |
| vite 实际编译 | ✅ 全部 200 |
| 无头实测（考试时间选择器） | ✅ 见上表 |
| 探针文件 | ✅ 用完即删（不留残留） |

**两项均未推送到远程**（用户要求等截图补齐后再一起推）。
