# P17 · 重新设计：取消随堂练习，只保留试卷（课程维度）

> 用户 2026-09-16 明确要求：**取消「在小节上配题 → 看完视频直接练习」这条设定**（理由：太麻烦），
> 只保留**试卷**功能。试卷以**课程**为维度组织（第一章检测、第二章检测…），
> 学生端在课程的两个位置都能看到它：**视频页右侧新增的「考试」页签** + **课程详情页**。
>
> 本文是**改动清单**：先对齐理解与边界，再动手。带 ❓ 的是需要用户拍板的点。

---

## 0 · 我理解的目标形态（先确认）

```
讲师：我的课程 → 建课向导（4 步，没有配题）
                 ① 基本信息 → ② 课程目录 → ③ 课时与视频 → ④ 讲师与上架
     考试管理 → 新建考试 → ① 基本信息（含「关联课程」）→ ② 组卷选题 → ③ 发布设置
                发布后 ↓
学生：该课程的视频页右侧「考试」页签 ──┐
      该课程的课程详情页「课程试卷」区 ─┴→ 点一场 → 答题页 → 交卷判分 → 看答卷
```

一句话：**「考试」从"课程内部的隐式附属物"升级为"课程维度的一等公民"**，
而"小节配题"这条旁路被彻底去掉。

---

## 1 · 学生端

| # | 改动 | 说明 |
|---|---|---|
| S1 | **清数据**：移除「Spring Boot 快速入门（免费）」的 **3.5 本章随堂测试** | 那是考试小节 `1002035`（`type=3`）+ 挂在它上面的卷 + `question_biz` 记录。❓删除范围见 §5 |
| S2 | **目录里每一项都是视频页** | 不再有"考试类型的小节"。学习页里「点 `type=3` 直接开卷」的分支保留（无害），但**不再产生**这种小节 |
| S3 | **视频页右侧加第 5 个页签「考试」** | 现页签：目录 / 问答 / 笔记 / AI助教（`learn.vue` 的 `tableBar`，`actId == 1..4`）→ 加 `{id:5,name:'考试'}` |
| S4 | 该页签内容 = **这门课已发布的试卷**列表 | 每行：卷名（第一章检测…）· 题量 · 总分 · 限时 · **我的状态**（未考 / 已交卷 得分 / 及格？）→ 点进**答题页** |
| S5 | **课程详情页加「课程试卷」** | `courseDetail.vue` 现页签：课程简介 / 课程目录 / 课程笔记 → 加「课程试卷」（同一数据源） |
| S6 | **答题页**：把 `/student/exams/answer` 从**引导页**改成**真答题页** | 现在是 125 行的纯引导文案。改为接 P14 已有的 `startExam(id)` / `submitExam(id, ...)` / `getExamResult(id)`，界面沿用 `Practise.vue` 那套（答题卡 + 提交 + 成绩条） |
| S7 | 交卷后 → 看答卷（复用 `/student/exams/review`，已接真接口） | 已有，无需改 |

**新接口（学生视角）**：`GET /es/exams/published?courseId=` → 该课程**已发布**试卷 + 我的作答状态。
> 为什么不复用 `/es/exams/page`：那是**讲师视角**（返回草稿、且没有"我考没考"）。学生只该看到已发布的。

---

## 2 · 讲师端

| # | 改动 | 说明 |
|---|---|---|
| T1 | **建课向导去掉第 4 步「试题与练习」** | 5 步 → **4 步**（重编号 ①基本信息 ②课程目录 ③课时与视频 ④讲师与上架）。页脚「已上架后补」等文案同步改 |
| T2 | 目录树的「配题 / 去配题 / 清除」按钮与选题面板一并移除 | 含 `openPicker` / `confirmPicker` / `removeQuiz` / `poolList` 等 |
| T3 | 上架校验清单去掉「配题」项 | `checks.quiz` 不再出现（后端 checks 也要去掉） |
| T4 | 课程预览里的「N 题练习 / 未配练习」标签移除 | `CourseOutline.vue` 的小节标签 |
| T5 | **出卷向导：两个「扩大范围」开关** | 现状**已经**默认按「关联课程」过滤（`pool.courseId = form.courseId`）→ 只需加两个开关：<br>① **从我的题库搜索**（跨课程，`creater=我`）② **从平台题库搜索**（全站，不带 courseId）<br>默认都关。开了就扩大范围，并明确提示"范围已扩大" |
| T6 | 发布后的试卷出现在学生端该课程的两个位置 | 依赖 S3/S4/S5 与本表的关联课程字段（已有 `form.courseId`） |

---

## 3 · 后端

| # | 改动 | 说明 |
|---|---|---|
| B1 | 新增 `GET /es/exams/published?courseId=`（学生视角，含我的作答状态） | 只返回 `status=已发布`；join `exam_record` 带上我的记录 |
| B2 | **停用**配题链路（而不是删） | `POST /es/practice/upsert`、上架的 `copySectionQuiz`、`question_biz` 同步 —— 这些是 P15 做的，**代码保留、前端不再露出、文档标注停用**。❓见 §5 |
| B3 | `checks` 去掉 quiz 项 | 后端与前端同步 |
| B4 | 题库可见范围（§4） | 独立一块，见下 |

---

## 4 · 你问的「怎么把题目变为平台可见」——我的提议

**只是改范围吗？是，但不能只加一个布尔列。** 建议按这套做（这就是 p13 里已经和你确认过的模型，现在正好是落地时机）：

### 4.1 数据模型
`question` 表加两列（迁移，幂等）：

| 列 | 含义 |
|---|---|
| `visibility` | `0` 私有（默认，只有作者可见） / `1` 公开（平台可见） |
| `publish_time` | 公开发布时间（用于排序：新公开的题排前面；撤回时清空） |

**现有题目一律置为公开**（否则全站讲师一觉醒来发现昨天能用的题全没了）。

### 4.2 交互
- 题目行上显示状态：**「仅我」/「公开」**（不能只在编辑页里改，要在列表就能一眼看到）
- 一个动作：**发布到平台** ⇄ **撤回**
- 撤回规则（复用既有规则，不新造概念）：
  - **没有被任何试卷引用** → 可直接撤回为私有
  - **已被引用** → **只能停用，不能硬撤**（别人的卷子里会突然少一道题）

### 4.3 为什么是"改范围"而不是"复制一份上交"（关键）
复制式上交会产生**两份近似重复的题**，然后没人敢用公共库。引用式共享只有一份：
作者改错别字，全站即时受益。唯一的风险是"别人用着用着我把题改了"——
**你们已有的「试卷发布才生成快照」规则正好挡住它**（P14 判分读的就是快照）。所以这个红利是白拿的。

### 4.4 平台题库**不靠管理员维护**
管理员判断不了答案对错（他只能审格式）。质量靠三件事：
1. **使用信号**（成本最低）：被 N 份试卷引用 / 被考过 M 次 / 正确率 X%
   —— P14 已经让"正确率"有真数据源了，正好接上
2. **众包纠错**：任何讲师可对别人的题提报问题，只有作者可见，作者一键处理
3. **策展人**（后期可选）：从资深讲师里选，合并重复、下架低质

### 4.5 落地顺序建议
**先做 4.1 + 4.2**（可见范围 + 发布/撤回 + 列表状态标记）；**信号展示随后**
（因为正确率要攒一段时间的作答才有意义，现在展示反而是一片 0）。

> 另外提一句：现在还**没有"试卷的可见范围"**——试卷一旦发布就是全站可见。
> 如果只想过"这门课的考试只给选课的人看"，那是另一个话题（要不要做由你定，本期建议不做）。

---

## 5 · 需要你拍板的 4 个点

1. **配题链路的处理**：
   - **(a) 推荐**：后端**保留但停用**（前端不再露出、文档标注），只清掉历史数据。
     理由：P15 那条链路已测试稳定，拆掉要动事务边界与 Feign 调用，风险 > 收益。
   - (b) 后端彻底拆掉（更干净，但要重测一遍上架与目录）
2. **删除数据的范围**（这几条都是 P15 我为演示造的，删除**不可恢复**，所以先问）：
   - 课程 1002 的考试小节 `1002035`「3.5 本章随堂测试」+ 挂在它上面的试卷
   - 课程 1002 的 1.1 小节配的随堂练习（3 题）及其 `question_biz` / 草稿引用
   - 两份 `exam_record` 作答记录（学生 8 的）
   - 题库那两道演示题**保留**（S6 的答题页正需要它们；`9001` 的解析乱码要不要顺手修，你上次没答）
3. **「考试」页签里考过的卷子**：显示得分 + 可回看答卷（推荐）？还是只显示"已交卷"？
4. **视频页与课程详情页的试卷区**：用**同一份数据、同一套卡片**（推荐，少维护一份）？

---

## 6 · 建议的执行顺序（3 批，每批可验收）

| 批次 | 内容 | 产出 |
|---|---|---|
| **第 1 批** | S1 清数据 + T1~T4 去掉配题（前端） + B2/B3 停用与 checks | 讲师端向导变 4 步；学生端不再有「练习」标签；**不破坏现有考试** |
| **第 2 批** | B1 新接口 + S3/S4 视频页「考试」页签 + S6 真答题页 | **学生在视频页能看到并完成一场考试**（核心闭环） |
| **第 3 批** | S5 课程详情页试卷区 + T5 两个扩大范围开关 + B4/S 可见范围（4.1+4.2） | 题目能发布到平台、能被跨课程引用 |

> 第 2 批是这次变更的**价值核心**（学生第一次能在课程里直接考试），建议优先验收。

---

## 7 · 实施记录（2026-09-16 三批全部完成）

用户回复「开工」并同意题目加「公开 / 仅我」范围切换。以下按批次记录实际做了什么、怎么验的。

### 第 1 批 · 清数据 + 讲师端去掉第 4 步

**数据清理**（`%TEMP%\p17-cleanup-backup.sql` 已备份，删除不可恢复）：

| 删了什么 | 明细 |
|---|---|
| 考试小节 | `course_catalogue` / `course_catalogue_draft` 的 `1002035`「3.5 本章随堂测试」（type=3） |
| 随堂练习卷 | `exam` 3 张（`exam_type=2`）：挂 1002035 / 1002011 / 1002012 的那三张 |
| 「练习」入口依据 | `question_biz` 里 `biz_id in (1002011,1002012)` 的 4 行（留了 1 条与本次无关的老数据） |
| 作答记录 | `exam_record` 3 条 + `exam_record_detail` 3 条 |
| 配题引用 | `course_cata_subject` / `course_cata_subject_draft` 里课程 1002 的 4 行 |

清理后核对：type=3 小节 0、`cata_subject` 0、exam(1002) 0、`exam_record` 0；`course.section_num=11` 与实际小节数一致（无需修正）。

**讲师端**：
- `courseWizard.vue`：整块第 4 步模板 + 选题面板 + 相关函数（`openPicker/confirmPicker/removeQuiz/poolList/pickerSelected` 等）删除；`STEPS` 5 → 4 项；页脚/上架结果文案改；`checks` 去掉 `quiz` 项；完成度权重重算（视频 30 → 40，总分仍 100）；进最后一步的 `n === 5` → `n === 4`；清理 `setSectionQuiz / pageQuestions / TYPE_LABEL / DIFF_LABEL` 四个不再用的 import。
- 后端 `buildChecks` 不再构造 `quiz` 项（Jackson NON_NULL → key 不出现；前端 `filter(Boolean)` 天然兼容）。
- `CourseOutline.vue` 去掉「N 题练习 / 未配练习」标签。

### 第 2 批 · 学生端（这次变更的价值核心）

**新接口** `GET /es/exams/published?courseId=`（学生视角）：只返回 `status=已发布` 的卷，
带 `questionCount / totalScore / duration / passScore / notice` 与**我的作答状态**
（`myRecordId / myStatus / myScore / myTotalScore / myPassed`，null = 还没考）。

**前端**：
- 新组件 `pages/student/components/CourseExamList.vue` —— **两处共用**（用户拍板：同一份数据、同一套卡片）。
  每行显示卷名 + 题量/满分/限时/及格 + 状态徽标（未作答 / 进行中 / `X / Y 分 · 及格`），
  点进答题页；已交卷的直接进答卷回看。
- `learn.vue`：右侧页签 `tableBar` 加第 5 项「考试」（`actId == 5`）。
- `courseDetail.vue`：`TABS` 加「课程试卷」（笔记那栏由 `v-else` 改成 `v-else-if`）。
- **`examAnswer.vue` 从 125 行的引导页重写成真答题页**（`?examId=`）：`startExam` 拿题（不含答案）→
  原生单选/多选交互（**选项编号 1 基**，与题库答案同口径）→ 未答题提示 → 交卷确认 → `submitExam` → 跳答卷回看。
  已交卷再进会被 400 拒绝，页面**识别「已经交过」并引导去看答卷**，而不是给一个空白答题区。
- `api/subject.js` 加 `pageCourseExams(courseId)`。

### 第 3 批 · 题目可见范围 + 组卷扩范围

**迁移** `deploy/mysql/p17-question-visibility.sql`（幂等，已执行）：`question` 加 `visibility`（0 私有 / 1 公开）+
`publish_time` + 索引；**存量题一律置公开**（可见集合只增不减），且只在「本轮刚加列」时执行（重复跑不会把别人新设的私有题改回公开）。

**后端**：`QuestionPageQuery` 加 `visibilityScope`；分页按 `visible`（公开 ∪ 我的）/ `public` / `mine` 过滤；
`PUT /es/questions/{id}/visibility`（仅作者；**撤回被引用的题被拒绝并把引用它的试卷名报出来**）。

**前端**：
- 题库列表：题干前加范围徽标（**公开 / 仅我**），操作列加**发布 / 撤回**；分段计数与列表用**同一口径**。
- **去掉了那道「假门槛」**：原来 `boundSchool ? 'all' : 'mine'`，而 `boundSchool` 后端写死 true → 门槛从未生效（P13 §0）。
- 出卷向导第 2 步加「扩大搜索范围」两个开关（我的题库 / 平台题库），默认关；
  打开时**课程筛选置灰**并提示"已跨课程"，避免"选了课程却搜出全部"的困惑。

### 实测

| 场景 | 结果 |
|---|---|
| `p17` 迁移 | ✅ 执行成功 + **重复执行幂等**；3 道题全部置公开 |
| 学生视角课程试卷 | ✅ 课程 1002 → 1 场「第一章检测 · Spring Boot 基础」；不传 `courseId` → 400；无试卷的课程 → `[]` |
| 撤回自己的题（未被引用） | ✅ 成功，`refCount=0` |
| 撤回别人的题 | ✅ 400「这道题不是你出的，不能改它的可见范围。」 |
| **撤回被引用的题** | ✅ 400「这道题已被 1 份试卷引用（第一章检测 · Spring Boot 基础）。撤回会让那些卷子少一道题，请先处理引用。」 |
| 三种口径题量（讲师2） | ✅ `visible=3`（公开 2 + 我的私有 1）、`mine=2`、`public=2` |
| **学生端视频页「考试」页签**（无头实测） | ✅ 页签变 `[目录\|问答\|笔记\|AI助教\|考试]`；点开后 pane 显示「第一章检测 · Spring Boot 基础 未作答 共 1 题 · 满分 10 · 限时 30 分钟 · 及格 10 分 开始答题」 |

> 无头实测的一个方法细节：本次登录态是**用真实登录拿到的 token 注入 sessionStorage**（同源 iframe 共享）——
> vite 不代理 `/as`（直接 `curl` 那个前缀是 404），所以探针里 fetch 登录走不通。含 token 的探针**用完即删**。

### 遗留

- ⏳ 学生端「考试」页签与课程详情页试卷区的**浏览器观感**建议用户点一遍（数据与交互已实测通过）。
- ⏳ 配题链路的后端代码（practice upsert / 上架搬 `course_cata_subject` / `question_biz` 同步）**按计划保留未拆**，
  仅前端不再露出、历史数据已清 —— 属"停用"状态，见 §0 与 §5 的决策。
- 题库现有 3 道题可演示（含用户自己录的一道）；课程 1002 有 1 张已发布试卷。

---

## 8 · 用户测试反馈修复（2026-09-16 晚）

用户实测：学生端考试、讲师端、考试页签均 OK。唯一问题在建课向导侧栏 —— **显示「共 5 步」且完成度 60%**，期望「共 4 步」与 25 / 50 / 75 / 100。

### 根因（两个都是 P17 改 5 步 → 4 步时漏掉的）

| 现象 | 根因 |
|---|---|
| 顶部仍写「共 5 步」 | `<p class="cw-head__sub">共 5 步 · 当前第 …` 是**硬编码**；`STEPS` 改成 4 项后这里没跟着改。`v-if="step < 5"`（下一步按钮）同样写死 |
| 完成度 60% | 进度算法按**内容填充度加权**（基本信息 20 + 目录 30 + 视频 40 + 讲师 10）。编辑一门已建好的课程时，人还在第 1 步，目录与视频的历史进度已把数字抬到 60% —— 看着就像算错了 |

### 改法

1. 步骤数一律跟着 `STEPS` 走：`共 {{ STEPS.length }} 步`、`v-if="step < STEPS.length"`。
2. 完成度改语义为「**流程走到哪了**」：`percent = Math.round(step / STEPS.length * 100)` → **25 / 50 / 75 / 100** 四个台阶。
   中间试过「按已完成步骤数计分」，但对编辑中的课程仍会给出 50%（第 1 步却已完成 2 步），不符合用户预期，故取"走到第几步"。
   三件事各归各位：百分比 = 走到哪，步骤项 ✓ = 哪步完成，各步自己的校验清单 / 视频进度 = 还差什么。

### 实测（无头，真实登录态注入 sessionStorage）

```
header=共 4 步 · 当前第 1 步「基本信息」
cap=完成度   pct=25%   fillWidth=25%
steps=["① 基本信息","② 课程目录","③ 课时与视频","④ 讲师与发布"]
actions=["存草稿","下一步"]
```

> 方法细节：**教师端路由无全局守卫** → 无 token 也能渲染页面（接口报 401 而已）。
> 本次改的正是纯前端计算（只依赖 `step`），所以注入学员 token 即可验证；
> 教师端 admin 登录通道**要验证码**，无头拿不到（探针里 fetch 登录也走不通，vite 不代理 `/as`）。
