# P38 · 答题页为什么没有倒计时（以及一个更要紧的发现）

> 2026-09-18 · 用户新建考试后问：「学生界面这个考试页面没有计数显示，是正常现象吗？」
> （澄清：「计数」指的是**时间倒计时**）

---

## 1 · 结论先说

**不是 bug，但也谈不上"正常" —— 倒计时是一个从没做过的功能，两端都没有。**

排查中顺带发现一件**更要紧**的事：

> 🔴 **考试的开始 / 结束时间目前完全没有被强制。** 不在时间窗口内也能进考场，限时到了也不拦、不自动交卷。
> 也就是说，你在步骤③填的那两个时间，**当前只是展示给讲师看的，对学生没有任何约束力。**

证据见 §3。

---

## 2 · 用户的这个考试实例（说明"窗口不被强制"）

刚创建的考试在库里的样子：

```
id=2100805267016884226  name=demo1  course_id=1002  status=1(已发布)
start_at=2026-09-18 12:31:00   end_at=2026-09-18 12:32:00
create_time=2026-09-18 12:33:01      ← 创建时窗口就已经结束了
```

**窗口在创建时就已经过期**，但学生端**照样进得去、照样能作答**（`GET /es/exams/published?courseId=1002`
把它正常返回，`myStatus=0` 表示"进行中"）。这直接印证了 §1 的结论：**没有任何一处校验它**。

---

## 3 · 排查证据（逐条实测，不是推测）

| # | 检查项 | 结果 |
|---|---|---|
| 1 | 答题页有没有倒计时代码 | ❌ 全文件搜 `倒计时 / 剩余 / timeLeft / remain / countdown / setInterval` —— **只有一处命中**，是静态文案「限时 90 分钟」 |
| 2 | 后端返回的 `ExamStartVO` 有没有"截止时间" | ❌ 有 `duration`（分钟）、`passScore`、`notice`，**没有 `endAt` / `deadline`** |
| 3 | `exam_record` 表有没有截止字段 | ❌ 只有 `start_time` / `finish_time`，**无截止列** |
| 4 | 开始考试时校验「现在是否在窗口内」 | ❌ `ExamAnswerServiceImpl` 里搜不到任何窗口/超时相关校验 |
| 5 | 交卷时校验是否超时 | ❌ 同样没有 |
| 6 | 前端截图佐证 | ✅ `student-exam.png`：卷头是「共 2 题 · 总分 10 · **限时 90 分钟** · 及格 60 分」，右侧是「0 / 2 已作答」——**全页无计时器** |

> 所以这不是"某个考试的数据有问题"，而是**整条时限链路都还没建**。

---

## 4 · 影响分析（为什么值得补）

- **对演示**：考试系统的「限时」是评委/面试官最容易追问的点之一。现在卷头写着"限时 90 分钟"却没有计时器，
  细看会发现"说了但没做"，**比不写更容易被问住**。
- **对数据**：没有超时约束，学生可以无限期作答；`exam_record.finish_time - start_time` 也就不能反映真实用时。
- **对产品口径**：这正是 P37 里"结束时间早于开始时间要拦住发布"的延伸 ——
  当时只保证了"窗口本身是合理的"，但**窗口是否被遵守**是另一件事，两者都缺才算完整。

---

## 5 · 如果要补，怎么补（工作量不大，但需要先定一个口径）

### 先定口径：倒计时以哪个时间为准？

| 口径 | 含义 | 特点 |
|---|---|---|
| A. 进入时刻 + 考试时长 | 每人从点「开始答题」起算 90 分钟 | 常见于线上考试；但同一场考试每人结束时间不同 |
| B. 考试统一结束时间 | 所有人都在 `end_at` 截止 | 便于统一收卷；但晚进来的人时间被压缩 |
| **C. 两者取更早**（推荐） | `min(开始作答时间 + 时长, 考试结束时间)` | 真实考试系统的通行做法，两种情况都不会漏 |

### 后端（约 3 处，改动小但要重建 exam-service）

1. `ExamStartVO` **显式返回权威截止时间** `deadline`（按上面口径 C 计算）。
   ⚠️ 必须由接口返回，不能让前端自己拿 `duration` 算 —— 项目规矩是「服务端衍生的字段必须由接口显式返回」，
   而且前端算的时间可被篡改/绕过。
2. **进入校验**：不在 `[start_at, end_at]` 内时拒绝开始作答，报中文原话（走既有 `BadRequestException`）。
3. **交卷校验**：超过 `deadline` 时不再按正常交卷处理（或标记为超时交卷），至少不能让"超时无限作答"成立。

### 前端（`pages/student/examAnswer.vue`）

4. 卷头加倒计时（`setInterval` 每秒），剩 5 分钟变警示色；
5. 到点**自动交卷**（并有明确提示），而不是静默停在页面上。

> ⚠️ 自动交卷属于"替用户做决定"，建议**先弹一个提示再自动提交**，并保留一个短暂宽限期。

---

## 6 · 待用户决策

1. **要不要做？**（做 = 上面后端 3 处 + 前端 2 处；不做 = 记入未完成清单）
2. **口径选哪个？**（推荐 C：两者取更早）
3. 若做，**`student-exam.png` 需要重拍**（现在这张没有倒计时）。

---

## 7 · 顺带记录

- `student-exam.png` 与 `student-notice.png` / `teacher-marking.png` / `teacher-chat.png` 已补齐并接入 README（共 11 张）。
- 仍缺（可选）：`teacher-stats.png`、`teacher-wizard.png`、`teacher-exam-new.png`。
