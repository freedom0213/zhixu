# P26 · 目录时长不准 / 没有「已学完」标记 / 状态一直「未开始」

> 2026-09-17 · 用户报三个问题。查下来是**三个独立 bug**，其中一个是前端语法错误、一个是我 P25 之前埋的隐患。
> 好在用户已经实测留下的数据证明：**学习完成本身是生效的**（`learning_record.finished=1`，1.1 与 1.2 都完成了）。

## 1 · 目录里 1.1 显示 15:00，实际视频只有 5 秒

**根因有两层**：

| 层 | 问题 |
|---|---|
| 数据 | 1.1 的 `course_catalogue.media_duration` 是 **900**（课程预置时长），而挂上去的视频是 5 秒的样例。预置值与真实视频无关 |
| 链路 | 上传登记只传 **分钟**：`seconds = durationMin * 60`，而前端传的是 `Math.round(真实秒数 / 60)` → **5 秒被记成 1 分钟**（1.2 就是这样，真实 55.76 秒被记成 60） |

**修改**：
- `SectionVideoDTO` 增加 `durationSec`（真实秒数）；`saveSectionVideo` **优先用秒数**，没有才退回 `durationMin * 60`；
- 前端上传完成后把 `durationSec` 一并送去登记；
- 存量数据按 `media` 表的真实时长对齐：1.1 → **5 秒**、1.2 → **56 秒**（媒体表里是 55.7585）。

> 目录显示的时长现在与真实视频一致（`mmss(5)` = `0:05`）。

## 2 · 视频播完了，目录里没有「已学完 ✓」

**根因是前端一个语法错误**：`Catalogue.vue` 的 `startIcon()` 里第三个分支写成了**裸块**：

```js
if(未看完){ A } else if(已完成){ B } { C }   // ← 漏了 else，C 是独立块，总会执行
```

裸块无条件执行并覆盖前面的赋值 → `data` 永远是「未播放过」的图标 → **完成图标从来没显示过**。
→ 补上 `else`，三个分支语义分明（未看过 / 看过没看完 / 已学完）。

**顺带改善体验**：播到结尾时本地立即给该小节打上完成标记
（`sectionMap` 里存的就是目录里同一个对象），**不用整页刷新**就能看到 ✓。

## 3 · 讲师端「学生分析」一直显示「未开始」

**根因**：课表 `status` **只在「新学完一节」时才更新**（`handleLearningLessonsChanges`）。
只看了 10% 的学生 → 有播放记录但 status 仍是 `0 未开始` → 讲师端显示「未开始」，与事实不符。

**修改**：`addLearningRecord` 在「还没学完」的分支里加一步 **`markStarted(lessonId)`** ——
把课表从「未开始」推进到「学习中」（只动 `NOT_BEGIN`，不覆盖已学完 / 已过期）。

> 顺带说明：你之前看到「未开始」也可能是**另一位学生**（管理员那个账号在 1002 里确实没有学习记录），
> 以及页面数据是加载时的快照 —— 现在是真语义：**有播放进度 = 学习中**。

## 4 · 🔴 顺带揪出的重复累加 bug（课表进度虚高）

你的课表 `learned_sections` 已经是 **6**，但实际只完成 2 个小节。原因是延迟写库组件：

```java
public RecordCacheData(LearningRecord record) {
    this.finished = false;   // ← 硬编码
}
```

缓存里存的 `finished` **永远是 false** → 缓存命中的旧记录被反复当成「新完成」→ 同一小节被重复 +1。
→ 改成 `this.finished = record.getFinished() != null && record.getFinished();`
→ 并把你的课表进度修正回真实值（**2**）。

## 5 · 验证

| 场景 | 结果 |
|---|---|
| 目录时长 | ✅ 1.1 = **5**、1.2 = **56**（与真实视频一致） |
| 目录完成标记 | ✅ 1.1 / 1.2 均返回 `finished: true` |
| 对**已完成**小节连报 3 次 | ✅ `learned_sections` 仍为 2（不再重复累加） |
| 未完成但有进度（10%） | ✅ 课表 `status` 0 → **1（学习中）**，`learned_sections` 仍为 0 |
| 前端 SFC / vite 校验 | ✅ 0 错误 / 200 |

> 我为验证制造的记录已删除、临时改动的状态已复位。

## 6 · 你需要做的 & 遗留

- **强刷页面**（`Ctrl+Shift+R`）后重测：目录时长、播完是否出现 ✓、讲师端「学生分析」的状态。
- 如果 1.2 那个视频你只播了 52 秒就停下（记录里有 `moment=52`），那是**它被记成完成**还是中途停的，
  取决于当时的 `duration` —— 现在用的是播放器真实时长（55.76 秒），52 秒 ≈ 93%，本来就该算完成。
- 仍未做：学习**时长**统计（热力图）—— `learning_lesson` 没有时长列。
