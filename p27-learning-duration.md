# P27 · 学习时长（热力图与「累计/日均学习」的数据底座）

> 2026-09-17 · 学生端的「学习足迹」热力图与「累计学习 / 日均学习」一直显示「—」，
> 因为后端没有任何**按天**的时长数据。本次补上这条链路。

## 1 · 为什么不能复用现有表

| 表 | 有的 | 缺的 |
|---|---|---|
| `learning_lesson` | 学了几节、最近学习时间 | **没有时长列** |
| `learning_record` | 每节的最后播放坐标 | 只有坐标，推不出「每天学了多久」 |

热力图要的是**按天**的投入，所以单开一张按 `(user, course, date)` 聚合的表。

## 2 · 数据模型

```sql
learning_duration(user_id, course_id, learn_date, duration_sec)
UNIQUE KEY uk_user_course_date (user_id, course_id, learn_date)
```
迁移：`deploy/mysql/p27-learning-duration.sql`（幂等）。写入用 `INSERT ... ON DUPLICATE KEY UPDATE duration_sec = duration_sec + ?`，
唯一键保证**同一天只累加、不会写出多行**。

## 3 · 埋点口径（关键）

在 `addLearningRecord` 的「未完成」与「完成」两条路径**之前**统一累加（没看完也要计时长）：

> **净观看时长 = 播放坐标的推进量，单次最多 30 秒**（上报间隔 15 秒的 2 倍）

| 场景 | 行为 |
|---|---|
| 正常观看（每 15 秒上报，坐标推进 ≈15） | 如实计入 ✅ |
| 拖进度条（坐标一次跳几千秒） | 被上限截断（最多 30 秒），**不按跳过量算** |
| 暂停 / 挂机（坐标不动） | 计 0 ✅ |
| 漏报一次（坐标推进 30） | 计 30 ✅ |

**基线取 Redis 缓存里的记录**（`queryOldRecord` 优先命中缓存）—— 库里那份是延迟写库的产物，常常滞后一两次上报，
拿它当基线会把同一段进度反复计入（实测会虚高一倍以上）。

### 走过的弯路（记录以免重犯）
一开始想用「距上次上报的真实时间差」再取一次最小值（更贴近真实时间）。实测发现两条路都走不通：
1. 缓存的 JSON 里**没有时间戳**（只有 id/moment/finished）→ 时间差恒为 null；
2. 改用库里 `update_time` 也不行 —— 那是**延迟写库**的时间，滞后于真实上报。

最后收敛到「只按坐标推进量 + 上限」——简单、可预期，且对正常观看是准确的。

## 4 · 接口

`GET /ls/learning-durations/summary?days=140` →

```json
{
  "totalSec": 60,        // 累计（全部时间）
  "todaySec": 60,        // 今天
  "windowSec": 60,       // 窗口内合计
  "activeDays": 1,       // 窗口内**有学习**的天数
  "avgDailySec": 60,     // 日均 = 窗口内合计 ÷ 有学习的天数（不被空白天拉低）
  "days": [{ "learnDate": "2026-09-17", "durationSec": 60 }]
}
```
汇总数字一律**从按天明细推导**（单一数据源）。

## 5 · 前端

| 文件 | 改动 |
|---|---|
| `api/class.js` | 新增 `getLearningDurations(days)` |
| `pages/student/dashboard.vue` | 热力图强度由「是否打卡」改为**当天学习时长**；「累计学习」「日均学习」填真值；图例补到 4 档 |
| `pages/student/records.vue` | 同上（「累计学习时长」+「学习足迹」） |

**分档**（原先代码里就写着"接入时长后在此处按分钟分档即可"，现在落实）：

| 档 | 时长 | 色 |
|---|---|---|
| lv0 | 无学习 | `#edeef3` |
| lv1 | < 15 分钟 | `#cfe3fa` |
| lv2 | 15–60 分钟 | `#7fb0e8` |
| lv3 | ≥ 60 分钟 | `#0066cc`（主色） |

悬停提示直接给具体时长（`2026-09-17 · 15 分钟` / `… · 未学习`）。
**签到是独立功能**（连续打卡统计仍用签到数据），两者不再混在一个格子里。

## 6 · 实测

| 步骤 | 结果 |
|---|---|
| ① 首次上报（建基线） | ✅ 计 0 秒 |
| ② 坐标 0 → 15 | ✅ 累计 15 秒 |
| ③ 坐标 15 → 30 | ✅ 累计 30 秒 |
| ④ **拖进度条** 30 → 330 | ✅ 累计 60 秒（增量 300 被截到 30，**未虚高**） |
| ⑤ 暂停不动 | ✅ 保持 60 秒 |
| 汇总接口 | ✅ `{totalSec:60, todaySec:60, activeDays:1, avgDailySec:60, days:[…]}` |
| 前端 SFC / vite | ✅ 0 错误 / 200 |

> 测试数据已清空（`learning_duration` 归 0）。

## 7 · 说明 / 遗留

- **历史格子会是空的**：埋点是本次才加的，之前的播放没有时长记录 —— 热力图**从今天起**开始积累。
- 「学习明细」里按真实最近学习时间分组的部分未改动。
- 学生分析（讲师端）的「学习时长」列现在**有数据源了**，可以补上；本次未动（避免扩范围）。
