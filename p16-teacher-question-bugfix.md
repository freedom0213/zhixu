# P16 · 录题链路三个 bug 的根因与修复

> 触发：2026-09-16 用户在教师端实测，报了三个现象：
> ① 新建题目页「题型 / 难度」选项点了没反应；
> ② 填完信息保存后，题目没有出现在题库的「全部题目」里；
> ③ 建课向导第 ④ 步配题，点「从题库选题」找不到那道模拟题（Nacos 那道）。
>
> 三个都复现了，**根因互不相同、但有一条共同的"看不见的线"**：题目写进去了，
> 却因为一个不存在的分类 id 把**整个题库列表**打挂 —— 于是"保存没进库"和"配题找不到题"
> 看起来像两个问题，其实是同一个 500。

---

## 0 · 结论速览

| # | 现象 | 根因 | 性质 |
|---|---|---|---|
| ① | 题型/难度点了没反应 | `.q-chip` 样式**只写在 `questions.vue` 的 `<style scoped>` 里**，录题页完全没定义 → 按钮是裸元素，`is-on` 也没有视觉差异 | 纯样式缺失（**功能一直是好的**） |
| ② | 保存后不在列表 | 两层叠加：**(a)** 后端要求解析 5–300 字，前端不校验、空解析传 `''` 被拒；**(b)** 前端把 `cateIds` 用 courseId 占位 → 写进不存在的分类 → **列表接口 NPE、整页 500** | 一个真实的 500 + 一个不可读的报错 |
| ③ | 配题面板找不到题 | 面板按 `courseId` 过滤**是对的**（那道题 `course_id=1002` 能匹配），但 `/questions/page` 已经 500 → catch 后列表为空 | ②的连带伤害 |

---

## 1 · 现象①：题型/难度"点不动"

### 根因
`questionEdit.vue` 用了 `.q-chip`（题型 / 难度 / 知识点三处按钮），但
**全仓只有 `pages/teacher/questions.vue` 的 `<style scoped>` 里定义过它**。

`<scoped>` 只作用于组件自己的模板 → 录题页里的按钮**拿不到任何样式**：
没有胶囊底、没有圆角、`is-on`（选中态）也没有任何视觉差异。
**功能其实一直是好的**（`form.type` / `form.difficulty` 真的变了、提交也带着新值），
但用户看到的是一排"点了没反应"的文字。

### 修法
把 `.q-chips` / `.q-chip` / `.q-select` **上移到全局 `src/style/teacher.scss`**（挂在 `.teacherShell` 下，
与 `.q-btn` / `.q-act` 同一位置——它们本来就是同一类"跨页面复用的基础控件"），
并从 `questions.vue` 删掉重复定义，原地留了一段说明防止有人再写回去。

### 实测（无头，computed style 客观判定）
```
INIT   single = rgb(0,102,204) is-on=true      multi = rgb(245,245,247) is-on=false
CLICK multi  → multi  = rgb(0,102,204) is-on=true   | single = 灰 is-on=false
CLICK single → single = rgb(0,102,204) is-on=true   | multi  = 灰 is-on=false
CLICK hard   → hard   = rgb(0,102,204) is-on=true
```
> ⚠️ 量测时踩了一个已知的坑，记在这里免得下次再踩：
> **headless 的 virtual-time 不驱动 CSS transition** → 不注入 `*{transition:none!important}`
> 的话，颜色会"冻结"在初始状态，读出来的是假象（第一次测就误判成"样式没生效"）。
> 项目无头实测经验里本来就有这条，这次漏了。

---

## 2 · 现象②：保存后没进"全部题目"

### 根因 (a)：解析的 5–300 字门槛
后端 `QuestionFormDTO.analysis` 带 `@Size(min = 5, max = 300)`：

| 传什么 | 结果 |
|---|---|
| `"测试解析"`（4 字） | ❌ 400「答案解析长度为5-300」 |
| `""`（空串，长度 0） | ❌ 400 —— **`@Size` 拒空串，只放行 `null`** |
| `null` / 不传 | ✅ 通过（`question_detail.analysis` 列可为 NULL） |
| ≥5 字 | ✅ 通过 |

而前端 `validate()` **完全没有校验解析**，且适配层 `analysis: data.analysis || ''` 把"没填"
写成了空串 → **越是"先不写解析"，越存不进去**。

更糟的是 `createQuestion` 用的是 `unwrap`（不是 `call`），所以 400 时前端 toast 出来的
是 axios 的英文原文 `Request failed with status code 400` —— 用户根本看不出是解析的问题。

### 根因 (b)：`cateIds` 用 courseId 占位 → 列表整页 500
这是**最严重的一个**。链路：

```
前端 toQuestionForm:  cateIds = [courseId, courseId, courseId]     ← 用课程 id 占位
        ↓
后端 addQuestion:     cateIds.size() >= 3 → cate_id1/2/3 = 1002/1002/1002
        ↓
列表 QuestionServiceImpl: categoryCache.getCategoryNameList(List.of(cateId1, cateId2, cateId3))
        ↓
CategoryCache:        map.get(1002) → null → null.getName() → NullPointerException
        ↓
GET /es/questions/page → 500「服务器内部异常」  ← 题库整页打不开
```

两个让它更毒的地方：

1. `List.of(...)` **本身就不接受 null 元素** —— 所以哪怕三个分类都是 NULL（而不是不存在的 id），
   也会在进入循环前就抛 NPE。
2. 分类（`cate_id1/2/3`）是**历史字段**，教师端录题表单里选的是**课程**（`courseId`），
   根本没有分类可选。也就是说：**老师每录一道题，题库列表就被打挂一次**。

### 修法（三层，缺一层都还会复发）

**后端 · `CategoryCache`（容错，必须）**
`getCategoryNames` / `getCategoryNameList` / `getNameByLv3Ids` / `getNameByLv3Id` 全部加了保护：
id 为 `null` 或指向未维护的分类时，跳过 / 给空串占位，**不再抛空指针**。
（最后两个是"一级/二级/三级"全路径，原来是 `map.get(...).getParentId()` 一路点下去，同样一碰就崩。）
> 原则：**一条脏数据不该让整页打不开**。

**后端 · `QuestionServiceImpl`**
- 两处 `categoryCache.getCategoryNameList(List.of(cateId1, cateId2, cateId3))`
  → 改用私有 `cateIdsOf(q)`：**先过滤 null 再传**（`List.of` 不许有 null）。
- `addQuestion` 不再强制"必须关联三级分类"：
  `cateIds` **允许为空**（题目归属走 `courseId`），传了才要求凑够 3 个。

**前端 · `api/teacher/questions.js`**
- `cateIds: []` —— **绝不用 courseId 占位**，并写明原因。
- `analysis: trimmed || null` —— 空解析传 `null`（不是 `''`）。
- 新增统一的 `call()`：把服务端 `text/plain` 的**中文原文**抛出来
  （`答案解析长度为5-300` / `服务器内部异常`），替换掉原来所有 `unwrap` 的裸用。
  这条与课程/考试接口层是同一个纪律（契约 §18.4）。

**前端 · `questionEdit.vue`**
- `validate()` 增加解析长度校验（**选填，但写了就 5–300 字**），字段下方给出提示与错误。

---

## 3 · 现象③：配题面板找不到题

面板的过滤逻辑其实是**对的**：

```js
pageQuestions({ courseId: draft.basic.courseId || getCurrentCourseId(), scope: 'all', size: 50 })
```

`draft.basic.courseId` 在适配层里是有的（= 1002），那道 Nacos 题 `course_id` 也是 1002 → 本应查到。
**它查不到，是因为 `/questions/page` 已经 500 了**（现象②b），catch 之后 `poolList = []`。

修完 ②b 之后实测：按 `courseId=1002` 过滤返回 **2 道题**（两道都在）。

---

## 4 · 实测汇总（exam-service 重建后）

| 场景 | 结果 |
|---|---|
| 删掉联调时造的脏数据题 | ✅ `DELETE` 后 `/questions/page` 从 500 → **200** |
| 旧题（`cate_id` = 1/2/3 真实分类） | ✅ 正常显示 `categories: ['后端开发','Java','微服务']` |
| **用新前端 payload 保存**（`cateIds: []`、`analysis: null`） | ✅ 200，入库；`cate_id1/2/3` 全 `NULL` |
| **新题入库后再读列表** | ✅ 仍 200（`total=2`），新题 `categories: []` —— **不再打挂列表** |
| 配题面板口径（`courseId=1002`） | ✅ 返回 2 道题 |
| 录题页题型/难度点击（无头 computed style） | ✅ 见 §1 |

---

## 5 · 留下的数据与后续

- **题库现有 2 道题**：`9001`（Nacos，P11 时期造的，`course_id=1002`）+
  `2100149423838253057`（【演示】Spring Boot 自动配置…，本轮验证时建的，`creater=2`、无解析、`course_id=1002`）。
  两道都能直接用于配题演示；不想要就删演示那条。
- ⚠️ **`9001` 的解析在库里是乱码**（P11 写入时字符集没对上），题干正常。
  不影响配题与判分，但学生看解析会看到乱码 —— 要修的话重录一次最省事。
- ⏳ **仍未做**：题库「全部题目」的可见范围机制（p13，默认私有 + 主动公开 + 可撤回）。
  现在能查到的题默认就是全站可见，那是 p13 要解决的事。

---

## 6 · 追加修复：课程「预览」永远说"没有目录数据"（09-16 用户实测）

### 现象
「我的课程」点任一课程的**预览** → 显示「这门课还没有目录数据 / 课程的章节、小节内容由课程服务提供，
该接口尚未接入（契约 §13）」。而那门课（Spring Boot 快速入门（免费））**明明有 3 章 12 节**。

### 根因（两层，第二层是"陈旧注释误导了实现"）
1. `CourseOutline.vue` 读的是 `course.outline`，而 `listMyCourses()` 里 **`outline: null` 是写死的**
   —— 课程列表接口**不返回目录明细**。所以**任何课程**的预览都必然显示空态，与课程有没有目录无关。
2. 那条空态文案本身也是**过时且不成立**的：说"接口尚未接入（契约 §13）"，
   但 §13 的建课九接口 P11 就做完了，目录一直在库里（草稿表 / 正式表都有）。

### 修法
预览改为**按需拉取**：点「预览」时调 `getCourseDraft(courseId)`，把返回的 `chapters` 传给组件
（形状与组件期望的 `outline` 完全一致：`{id,title,sections:[{id,title,duration,video,quiz,preview}]}`）。

为什么用**草稿口径**而不是学生端的 `/cs/courses/{id}/catalogs`：讲师预览的是"**我要发布的样子**"，
要与建课向导第 ② 步看到的一致（含还没上架的改动）。

组件侧顺带把空态拆成**三种**（原来只有一种笼统说法）：
- 加载中 → 「正在读取课程目录…」
- 拉取失败 → 显示**服务端原话**（走 `getCourseDraft` 的 `call()`）
- 真的没有目录 → 「章节与小节要在建课向导第 ② 步建立」

### 实测
`getCourseDraft(1002)` → **3 章 12 节**（第一章 3 节 / 第二章 4 节 / 第三章 5 节），
即预览抽屉会显示这 12 节。SFC 校验与 vite 按需编译均通过。

---

## 7 · 两个产品语义问题（用户同日提出，**待拍板，尚未改**）

### 7.1 题目"必须在所属课程里才能被选到"——与既定规则冲突
现状：录题必须选课程；**配题面板按 `courseId` 过滤** → 一道题只能被它所属的那一个课程引用。

用户自己的评价是"还行"（逼老师录题时就想清归属）。但要注意它与**已拍板的规则冲突**：
题库是**独立一级导航**、**题目可跨课程 / 跨试卷复用**。会疼的具体场景：
- 一位老师教两门课，《自动配置》那道题两门都用得上 → 现在必须录两遍 → 题库里出现两道近似重复题
  （而这正是"不要复制式上交"要避免的东西）
- 一门课拆成两门 → 题目全留在旧课程上
- p13 要做的"平台共享题"——跨课程复用是它的前提

**建议**：默认仍按当前课程过滤（好找、防误选，符合用户意图），但加一个「从平台题库搜索」开关，勾上即跨课程搜。
后端本来就不传 `courseId` 就是全量，代价很小。

### 7.2 小节上配的题 → 为什么会出现在"考试管理"里
用户困惑："在小节上分配题目是为了学生看视频时能看到题吗？既然是小题，为什么考试管理里有记录？到底是小题还是考试？"

**答案**：功能上它就是**随堂练习** —— 学生进那一节会看到「练习」入口（P15 打通的正是这条）。
它出现在考试管理里，是因为 **P15 的实现方式**：老师配题时系统**幂等生成一张 `exam_type=2`（随堂练习）的卷子**
挂到那一节上。用"试卷"这套表是为了**复用已做好的判分 / 批改 / 统计**（否则要再造一份判分逻辑）——
**是实现手段，不是产品概念**。

呈现上的问题：考试管理列表把**正式试卷和随堂练习混在一起排队**，只有副标题一行小字区分（P15 加的 `metaLine`）。
用户会困惑说明这个呈现不合格。备选：
- **A（推荐，代价最小）**：列表加类型筛选（全部 / 正式试卷 / 随堂练习），默认落「正式试卷」；
  随堂练习卡片写明「由课程小节配题生成 · 改题请去课程向导第 ④ 步」且**不给"编辑"入口**（改了也会被覆盖）。
- B：随堂练习完全从考试管理移出，只在课程编辑页可见。
- C：保持现状，只把文案写清楚。
