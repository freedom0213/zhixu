# P32 · 上架成功的反馈增强 + 冗余媒资清理

> 2026-09-18 · 用户反馈：更换 / 删除 / 新增视频的功能都没问题了，但「**提交上架后成功的感觉很薄弱**」，
> 希望给个明显提示。同批处理：清理无引用的重复视频。

## 1 · 为什么"薄弱"：那一块**根本没有样式**

`courseWizard.vue` 里的上架结果是这样写的：

```html
<div v-if="published" class="q-dialog">
  <div class="q-dialog__panel">
    <h3 class="q-dialog__title">课程已上架</h3>
    ...
```

而 `.q-dialog` 系列类名**在这个文件里没有任何样式定义**（全项目只有 `examWizard.vue` / `questions.vue`
各自定义过，样式是 scoped 的，不共享）→ 渲染出来就是**一段没有背景、没有面板、没有定位的裸文字**飘在页面上。
所以用户觉得"薄弱"是准确的：它连一个对话框的形态都没有。

## 2 · 重新设计：一个真正的结果卡

**信息顺序按重要性排**：先"成功了" → 再"上了什么" → 最后"下一步"。

```
        ╭──────────╮
        │    ✓     │   ← 60px 徽标：绿 = 已上架（学生已可见）／灰 = 仅存草稿
        ╰──────────╯      带一次轻微弹入（0.44s）
        课程已上架
     《Spring Boot 快速入门（免费）》

   ┌─────────┬─────────┬──────────┐
   │    3    │   11    │  8/11    │   ← 事实条：章 / 节 / 已配视频
   │   章    │   节    │ 已配视频  │      （没配齐时数字转暖色）
   └─────────┴─────────┴──────────┘

   还有 3 个小节没有视频：这些小节学生看不到。
   补上传后再点一次「提交上架」即可同步。      ← 一句针对当前数据的提醒

                    [留在本页] [去我的课程看看]
```

**四个状态的文案各不相同**（草稿态不能说"学生看不到" —— 整门课都还没上架，语义要如实）：

| 状态 | 提醒文案 |
|---|---|
| 上架 + 视频缺 | 还有 N 个小节没有视频：这些小节学生看不到。补上传后再点一次「提交上架」即可同步。 |
| 上架 + 视频齐 | 全部小节都已配好视频，学生现在能看到完整的课程内容。（绿色确认） |
| 草稿 + 视频缺 | 还有 N 个小节没有视频 —— 上架后这些小节学生看不到。可以先补齐再上架。 |
| 草稿 + 视频齐 | 草稿已保存，学生暂时看不到这门课。想让它上线，点「提交上架」。 |

**无障碍与交互**：
- `role="dialog"` + `aria-modal` + 动态 `aria-label`
- 出现时**自动聚焦**到卡片（键盘用户能直接读到），`Esc` 可关，点遮罩空白处可关
- `@media (prefers-reduced-motion: reduce)` 下关掉动画

**深浅兼顾**：徽标绿/灰用项目既有 `--s-ok`，其余全部走 `--s-ink / --s-soft / --s-hairline / --s-sh-pop` 令牌，
与教师端其它页面同一套语言；按钮复用全局 `.q-btn`。

## 3 · 自己先看过效果

写了一个等价的静态预览（把 SCSS 嵌套展开成普通 CSS）用无头 Chrome 截图核对三种状态
（已上架有缺视频 / 已上架全齐 / 存草稿），确认几何、间距、配色无问题后**才交付**；预览文件用完即删。
> 这一步发现并修掉了「存草稿却写『学生看不到』」的文案语义错误。

## 4 · 顺带的清理（你已授权）

删之前重新查过引用 —— **数据已经变了**（你刚做过一轮增删改测试），所以**没有**照上一轮的清单执行：

| media | 文件 | 大小 | 判定 | 处置 |
|---|---|---|---|---|
| 9 | Wuthering Waves…mp4 | 8 MB | **2.1 正在使用** | **保留**（上一轮我说它无引用，现在不是了） |
| 11 | Enderlilies…mp4 | 262 MB | 全库 0 引用 | **已删** |
| 13 | Enderlilies…mp4 | 262 MB | 全库 0 引用 | **已删** |
| 15 | Wuthering Waves…mp4 | 8 MB | 全库 0 引用（测试残留） | **已删** |

- 删除前逐表核对：`course_catalogue.media_id`、`course_catalogue_draft.media_id`、两表的 `video_id` ——
  四条查询**全部为 0**；另确认 `tj_exam / tj_learning / tj_user` 三个库都没有引用媒资 id 的字段。
- 结果：视频卷 **1.2 GB → 631 MB**（释放 **532 MB**），文件数 9 → 6。

> ⚠️ 删除不可恢复，所以我没有"照单执行"而是**重新查了一遍谁在用** —— 这正是上一轮 P31 学到的：
> **不能拿旧结论当依据，验证必须回到当前数据。**

## 5 · 验证

| 项 | 结果 |
|---|---|
| 在用视频流（4 个） | ✅ 落盘均为 1024 字节、文件头合法 mp4 |
| 学生端签名（2.1 / 2.2 / 2.3） | ✅ 均返回 `mediaUrl` |
| 视频卷 | ✅ 631 MB / 6 个文件 |
| `courseWizard.vue` SFC + vite 编译 | ✅ 0 错误 / HTTP 200 |
